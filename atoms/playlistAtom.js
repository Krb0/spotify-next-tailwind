import { atom } from "recoil";

export const playlistState = atom({ key: "playlistAtom", default: null });

export const playlistIdState = atom({
  key: "playlistIdState",
  default: "0jKVCLiGlVECSJGe1yeQ0q",
});
